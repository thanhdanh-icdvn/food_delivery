import tw, { styled } from 'twin.macro';

const Label = styled.label(() => [
  tw`inline-flex items-center justify-center px-4 py-2 font-bold text-tertiary bg-tertiary-100`,
]);
export default Label;
